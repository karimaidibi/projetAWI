import { CrudBenevoleDisplay } from './crud-benevole-display';

describe('CrudBenevoleDisplay', () => {
  it('should create an instance', () => {
    expect(new CrudBenevoleDisplay()).toBeTruthy();
  });
});
